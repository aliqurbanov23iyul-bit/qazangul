document.addEventListener('DOMContentLoaded',async()=>{const el=document.querySelector('[data-db]');if(!el)return;try{const r=await fetch('/api/status');const j=await r.json();el.innerHTML=`<span class="dot ${j.database?.connected?'ok':''}"></span>${j.database?.connected?'Database qoşulub':'Database qoşulmayıb'}`;}catch{el.innerHTML='<span class="dot"></span>Backend lokal rejimdə';}});function needsDb(){toast('Database qoşulduqdan sonra bu əməliyyat aktiv olacaq.');}